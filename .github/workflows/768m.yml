name: Build ImmortalWrt 24.10 (500M Free Space)

on:
  workflow_dispatch:

env:
  REPO_URL: https://github.com/immortalwrt/immortalwrt
  REPO_BRANCH: openwrt-24.10
  BUILD_DIR: /workdir/immortalwrt

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 释放磁盘空间
        uses: jlumbroso/free-disk-space@main
        with:
          tool-cache: true
          android: true
          dotnet: true
          haskell: true
          large-packages: true
          docker-images: true
          swap-storage: true

      - name: 初始化编译环境
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential clang flex bison g++ gawk gcc-multilib g++-multilib \
          gettext git libncurses-dev libssl-dev python3-distutils rsync unzip zlib1g-dev swig libpython3-dev
          sudo mkdir -p /workdir
          sudo chown $USER:$GROUPS /workdir

      - name: 克隆源码
        run: |
          cd /workdir
          git clone --depth 1 $REPO_URL -b $REPO_BRANCH immortalwrt

      - name: 修复 Feeds 与依赖逻辑 (防止 Syntax Error)
        run: |
          cd ${{ env.BUILD_DIR }}
          # 1. 添加 small-package 源
          echo "src-git small_package https://github.com/kenzok8/small-package" >> feeds.conf.default
          ./scripts/feeds update -a
          
          # 2. 物理删除冲突项 (保留官方 golang 目录以修复 golang/host 缺失)
          rm -rf feeds/packages/net/smartdns
          rm -rf feeds/packages/net/adguardhome
          rm -rf feeds/luci/applications/luci-app-smartdns
          rm -rf feeds/luci/applications/luci-app-passwall
          rm -rf feeds/luci/applications/luci-app-adguardhome
          
          # 3. 暴力修复语法错误 (剔除 Makefile 中可能存在的非法字符)
          find feeds/small_package -name "Makefile" | xargs sed -i 's/@//g' 2>/dev/null || true
          
          ./scripts/feeds install -a

      - name: 生成配置文件 (绝对路径 1G 分区)
        run: |
          cd ${{ env.BUILD_DIR }}
          rm -rf tmp
          
          cat > .config <<EOF
          CONFIG_TARGET_x86=y
          CONFIG_TARGET_x86_64=y
          CONFIG_TARGET_x86_64_DEVICE_generic=y
          CONFIG_GRUB_IMAGES=y
          CONFIG_GRUB_EFI_IMAGES=y
          
          # 分区设置：1024MB 实现 500MB 左右空闲
          CONFIG_TARGET_KERNEL_PARTSIZE=64
          CONFIG_TARGET_ROOTFS_PARTSIZE=1024
          
          # 核心插件
          CONFIG_PACKAGE_luci-app-passwall=y
          CONFIG_PACKAGE_luci-app-smartdns=y
          CONFIG_PACKAGE_luci-app-adguardhome=y
          CONFIG_PACKAGE_luci-app-syncdial=y
          CONFIG_PACKAGE_luci-app-mwan3=y
          CONFIG_PACKAGE_luci-app-ddns=y
          CONFIG_PACKAGE_ddns-scripts-cloudflare=y
          
          # 网卡驱动 (Intel e1000/e1000e/igb/igc)
          CONFIG_PACKAGE_kmod-e1000=y
          CONFIG_PACKAGE_kmod-e1000e=y
          CONFIG_PACKAGE_kmod-igb=y
          CONFIG_PACKAGE_kmod-igc=y
          
          # 系统基础
          CONFIG_PACKAGE_ipv6helper=y
          CONFIG_PACKAGE_curl=y
          CONFIG_PACKAGE_ca-bundle=y
          EOF
          
          make defconfig

      - name: 下载软件包 (处理 Hash Mismatch)
        run: |
          cd ${{ env.BUILD_DIR }}
          make download -j8 || make download -j1 V=s
          find dl -size -1k -exec rm -f {} \;
          make download -j1 V=s

      - name: 编译固件 (单线程保底解决 Code 2)
        run: |
          cd ${{ env.BUILD_DIR }}
          # 多线程失败后自动输出详细日志并单线程编译
          make -j$(nproc) || make -j1 V=s

      - name: 上传固件
        uses: actions/upload-artifact@v4
        with:
          name: ImmortalWrt_24.10_x86_500M_Free
          path: |
            ${{ env.BUILD_DIR }}/bin/targets/x86/64/*-combined.img.gz
            ${{ env.BUILD_DIR }}/bin/targets/x86/64/*-combined-efi.img.gz
